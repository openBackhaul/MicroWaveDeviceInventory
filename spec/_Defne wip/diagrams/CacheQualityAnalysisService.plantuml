@startuml 02x_CacheQualityAnalysisService
skinparam responseMessageBelowArrow true

title Cache Quality Analysis Service

participant "Analysis UI" as client
participant "/cache-quality-measurements" as store

note over client
User selects filters: by deviceId, vendor, or deviceType
end note

client -> store : GET /cache-quality-measurements?deviceId=...
store --> client : diff_summary for device

client -> store : GET /cache-quality-measurements/grouped-by-deviceType
store --> client : grouped_score_summary by deviceType

client -> store : GET /cache-quality-measurements/grouped-by-vendor
store --> client : grouped_score_summary by vendor

note over client
UI displays the summary of
device cache quality metrics
end note

@enduml

  ## == Provide mount-name list of MWDI ==========================================
  - forwarding-name: PromptForDeviceListCausesReadingFromElasticSearch
    uuid: mwdi-1-0-0-op-fc-is-050
    forwarding-type: InvariantProcessSnippet
    management-requests:
      operation-client-update:
      fc-port-update:
      fc-port-deletion:
      operation-client-deletion:
    initiating-requests:
      # request MUST be called with fields filter "?fields=node(node-id)"
      # unclear whether the fields filter therefore must/can also be provided here, or only in the OAS
      - server-name: /
        uuid: mwdi-1-0-0-op-s-is-020
    consequent-requests:
      - client-name: ElasticSearch
        uuid: mwdi-1-0-0-es-c-es-1-0-0-000
